What YOU need to do manually (one-time, via AWS Console with SSO):
Create IAM Role named sap-hana-backup-role with path /sap/

Trust: EC2, Backup, SSM services
Attach these 6 policies:
AmazonSSMManagedInstanceCore
AWSBackupDataTransferAccess
AWSBackupFullAccess
AWSBackupRestoreAccessForSAPHANA
AWSBackupServiceRolePolicyForBackup
AWSSystemsManagerForSAPFullAccess
Also attach a policy allowing secretsmanager:GetSecretValue on your POC-SAP-Hanna secret
Create Instance Profile named sap-hana-backup-role-instance-profile

Add the role sap-hana-backup-role to it
After creating those, re-run the pipeline.


When you create the IAM role sap-hana-backup-role in AWS Console, you attach 6 managed policies (the ones I listed). But your SAP HANA EC2 instances also need to read the secret POC-SAP-Hanna from Secrets Manager during the SAP registration step.

So on the same role (sap-hana-backup-role), add one more inline policy:

Steps:

Go to IAM → Roles → sap-hana-backup-role
Click Add permissions → Create inline policy
Switch to JSON tab
Paste this:
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "secretsmanager:GetSecretValue",
        "secretsmanager:DescribeSecret"
      ],
      "Resource": "arn:aws:secretsmanager:us-east-1:677277859823:secret:POC-SAP-Hanna*"
    }
  ]
}
Click Next → Name it allow-read-sap-hanna-secret → Create policy
Why? When the SSM document runs on EC2 instances, it executes:

aws secretsmanager get-secret-value --secret-id POC-SAP-Hanna
The EC2 instance uses the sap-hana-backup-role role. Without this policy, it will get "Access Denied" when trying to read the password.


----------------------------------------------

Your runner role poc-runner-role needs these EC2 permissions added. Ask your AWS admin to attach this inline policy to poc-runner-role:

{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "ec2:AssociateIamInstanceProfile",
        "ec2:ReplaceIamInstanceProfileAssociation",
        "ec2:DisassociateIamInstanceProfile",
        "ec2:DescribeIamInstanceProfileAssociations",
        "ec2:DescribeInstances",
        "iam:PassRole"
      ],
      "Resource": "*"
    }
  ]
}
Steps for your admin:

IAM → Roles → poc-runner-role
Add permissions → Create inline policy → JSON → paste above
Name it: allow-ec2-instance-profile-attach
Save
Without these permissions, the pipeline cannot attach the IAM role to your SAP HANA EC2 instances — which is a core requirement of your setup.

Alternatively, if you can't get this permission either, I can remove the ec2-role-attachment module and you'd attach the instance profile to each EC2 manually via AWS Console. Want me to do that?


------------------------------

poc-ssm-output-677277859823-us-east-1-an

----------------------------------------------------------------------------------
The IAM role sap-hana-backup-role trust policy doesn't allow AWS Backup to assume it.

Go to AWS Console → IAM → Roles → sap-hana-backup-role → Trust relationships → Edit trust policy and make sure it looks like this:

{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": {
        "Service": [
          "ec2.amazonaws.com",
          "backup.amazonaws.com",
          "ssm.amazonaws.com"
        ]
      },
      "Action": "sts:AssumeRole"
    }
  ]
}
The key part is "backup.amazonaws.com" must be in the Principal → Service list. If it only has "ec2.amazonaws.com", add the other two.

-------------------------------------

What you need to do:
1. Add permission to poc-runner-role:

IAM → Roles → poc-runner-role → Add permissions → Create inline policy → JSON:

{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": "iam:CreateServiceLinkedRole",
      "Resource": "arn:aws:iam::677277859823:role/aws-service-role/backup.amazonaws.com/*"
    }
  ]
}
Name: allow-backup-service-linked-role

2. Delete the existing S3 bucket:

S3 Console → poc-backupaudit-report → Empty it first (if needed) → Delete bucket

3. Re-run the pipeline

-----------------------------------

The permission might need a broader resource scope. Update the inline policy you created on poc-runner-role:

Replace it with this (Resource = *):

{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": "iam:CreateServiceLinkedRole",
      "Resource": "*"
    }
  ]
}
For the S3 bucket — it already exists. Did you delete it? If not, go to:

S3 Console → find poc-backupaudit-report → Empty bucket → Delete bucket

If you can't delete it because it's owned by another account or has a retention policy, tell me and I'll rename it in the code.

--------------------------------------------------

Summary — do these 2 things:
1. Create the service-linked role manually (run in AWS CloudShell):

aws iam create-service-linked-role --aws-service-name backup.amazonaws.com
(If it says "already exists" that's fine — it means the framework should work)

2. Delete the old log group (or it was renamed so should be fine now)

Then re-run the pipeline.

If the iam:CreateServiceLinkedRole error STILL happens after running the command above, it means there's an Organization SCP (Service Control Policy) blocking it. In that case tell me and I'll disable just the audit_manager module — the rest of the infrastructure will work fine without it.


----------------------

aws logs delete-log-group --log-group-name /aws/backup/sap-hana-backup
aws logs delete-log-group --log-group-name /aws/backup/sap-hana-backup-events
-----------------------

aws logs delete-log-group --log-group-name /aws/backup/sap-hana-backup-events
---------------------------------------
Both resources already exist from previous runs. Delete them:

1. Delete the Backup Framework:

AWS Console → AWS Backup → Frameworks
Find the framework with same controls (may be named differently from a previous attempt)
Delete it
Or via CLI:

aws backup delete-framework --framework-name sap_hana_backup_compliance_framework
2. Delete the S3 Bucket:

aws s3 rb s3://poc-backupaudit-report --force
Or in console: S3 → poc-backupaudit-report → Empty → Delete

Then re-run the pipeline.


Est. Credits Used: 0.75

Elapsed Time: 13s

No file chosen
----------------------------------------------------

The local file has -monitor but your GitLab is still showing -events. That means your GitLab isn't pulling from terraform-sap-backup-v2 branch.

But regardless, let me fix both issues and push:


3 tools

Pushed.

Two things you need to do:

1. Make sure your GitLab pipeline is using branch terraform-sap-backup-v2 (not the old terraform-sap-backup). The log group error shows -events which means your pipeline is running old code.

2. Delete the log group that still exists:

aws logs delete-log-group --log-group-name /aws/backup/sap-hana-backup-events
Then re-run the pipeline from the terraform-sap-backup-v2 branch.


Est. Credits Used: 1.32

Elapsed Time: 58s
-----------------------------------------------

Do these 2 things:

1. Delete manually in AWS Console (one-time):
AWS Backup → Backup plans → delete sap-hana-backup-backup-plan
AWS Backup → Vaults → delete sap-hana-backup-vault (remove recovery points first)
AWS Backup → Frameworks → delete the compliance framework
Systems Manager → Documents → Owned by me → delete all sap-hana-backup-* documents
Systems Manager → State Manager → delete all SAP associations
CloudWatch → Dashboards → delete sap-hana-backup-backup-dashboard
CloudWatch → Alarms → delete sap-hana-backup-* alarms
CloudWatch → Log groups → delete /aws/backup/sap-hana-backup-monitor
EventBridge → Rules → delete sap-hana-backup-* rules
SNS → Topics → delete sap-hana-backup-backup-notifications
S3 → empty & delete poc-backupaudit-report
2. Then run the pipeline:
GitLab → CI/CD → Run pipeline → wait for plan → click ▶ on apply

This will create everything fresh with Terraform tracking the state.

